language: python
python:
     - "3.6"

before script:
     - pip install -r requirements.txt
     - pip install django-crispy-forms

branches:
    only:
      - staging

services:
     - postgresql

script:
  - ./manage.py test
