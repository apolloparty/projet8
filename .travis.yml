language: python
python:
      - '3.8.2'

before_script:
      - pip install -r requirements.txt
      - pip install django-crispy-forms

branches:
    only:
        - staging

script:
      - ./manage.py test