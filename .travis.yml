language: python
python:
      - '3.8.2'

before_script:
      - pip install -r requirements.txt
      - pip install django-crispy-forms

branches:
    only:
        - staging

services:
  db:
    image: postgres
    ports: 
      - "5432"
    environment:
      - POSTGRES_DB=cleaned
      - POSTGRES_USER=fostin
      - POSTGRES_PASSWORD=Hacksounet3*

script:
      - ./manage.py test